---
layout: base.njk
title: Blog - All Posts
---

<h1>Blog Posts</h1>
{% if collections.posts and collections.posts.length > 0 %}
    <ul class="posts-list">
        {% for post in collections.posts | reverse %}
            <li class="post-item">
                <article>
                    <h2 class="post-title">
                        <a href="{{ post.url }}">{{ post.data.title }}</a>
                    </h2>
                    <time class="post-date" datetime="{{ post.date | htmlDate }}">
                        {{ post.date | simpleDate }}
                    </time>
                    {% if post.data.tags %}
                        <div class="post-tags">
                            Tags:
                            {% for tag in post.data.tags %}
                                {% if tag !== 'posts' %}
                                    <span class="tag">{{ tag }}</span>
                                {% endif %}
                            {% endfor %}
                        </div>
                    {% endif %}
                    {% if post.templateContent %}
                        <p class="post-excerpt">
                            {{ post.templateContent | stripHtml | truncate(200) }}
                        </p>
                    {% endif %}
                </article>
            </li>
        {% endfor %}
    </ul>
{% else %}
    <p class="no-posts">No blog posts yet. Check back soon!</p>
{% endif %}